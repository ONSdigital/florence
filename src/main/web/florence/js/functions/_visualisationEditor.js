/**
 * Editor screen for uploading visualisations
 * @param collectionId
 * @param data
 */

function visualisationEditor(collectionId, data) {
    // Build section for uploading single ZIP file (not using editAddFile function because we only want a single file with just the uri stored)
    

    // Bind submit and cancel click event
    

}
