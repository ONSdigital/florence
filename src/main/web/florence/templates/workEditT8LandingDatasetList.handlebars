{{#each this}}
  <div id="{{@index}}" class="edit-section__sortable-item">
    <p id="edition-title_{{@index}}" edition-url="{{this.uri}}">{{this.description.title}}</p>
    <button class="btn-markdown-edit" id="edition-edit_{{@index}}">Edit</button>
  </div>
{{/each}}